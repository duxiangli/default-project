# 派单日志（自主派单审计留痕）

> **router 写白名单①**：本文件是 router Agent 在自主派单时允许写入的两个文件之一（另一个是 `待签批清单.md`，见 `.opencode/agents/router.md` 权限白名单；`审批记录.md` 仅人类维护）。其余写入一律被 deny。
> 记录格式：`| 派单号 | 时间 | 事项摘要 | 人类A | R | C（最多3） | 派发 expert/<id> | 结论摘要/四态 | 需签批(Y/N) |`
> 新记录一律插入到文件末尾 `<!-- dispatch-log-end -->` 标记**之前**（先 Read 再 edit 追加）。
> **需签批=Y** 的条目必须同步进入 `待签批清单.md`（router 唯一另一个可写文件）。

| 派单号 | 时间 | 事项摘要 | 人类A | R | C | 派发 expert/<id> | 结论摘要/四态 | 需签批(Y/N) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DSP-20260925-0000 | 2026-09-25 00:00 | 体系初始化：自主派单启用 | 各岗首席 | 路由Agent | — | — | 配置就绪 | N |

<!-- dispatch-log-end -->