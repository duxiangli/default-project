# 1223 CI 首次跑绿 · 真实 GitHub Actions 运行留档

> 依据：`AP-20260925-1223`（文档与审计证据一致性，签批结论「有条件批准」）
> 条件：**CI 首次跑绿**（`.github/workflows/expert-guardrails.yml` 的 6 项确定性检查）后关闭本项。
> **本文件是权威证据**（真实 runner 结果）；同目录 `1223-CI首次跑绿-本地模拟-*.md` 为前置的本地干净检出模拟。

## 1. 权威结论

| 项 | 值 |
| --- | --- |
| Workflow | `expert-guardrails`（`.github/workflows/expert-guardrails.yml`） |
| Run ID | `36243238096` |
| 链接 | https://github.com/duxiangli/default-project/actions/runs/36243238096 |
| HEAD | `382a8364` |
| 触发 | push → main |
| Runner | ubuntu-latest + Node 22（`actions/setup-node@v4`） |
| **结论** | ✅ **success（6/6 步骤通过）** |

## 2. 六步明细（真实 runner 输出）

| # | 步骤 | 结果 | 关键输出 |
| --- | --- | --- | --- |
| ① | 体系一致性校验（14 节） | success | `结果：45 通过 / 0 失败` |
| ② | 自动化自测 | success | `结果：52 通过 / 0 失败` |
| ③ | 治理契约审计（`--strict`） | success | `契约审计：17 通过 / 0 警告 / 0 违规` |
| ④ | 签批闭环结构一致性（`--check`） | success | `签批状态视图：结构最新 ✓（未闭环/已闭环/孤儿 与台账一致）` |
| ⑤ | 度量看板结构一致性（`--check`） | success | `度量看板：结构最新 ✓（总量/四态/严重度/采纳率/数据质量 与台账一致）` |
| ⑥ | 签批超期/孤儿卡口（`--strict`） | success | `门禁通过：无超期、无孤儿签批` |

> 注：③ 在 Linux 上是「17 通过 / 0 警告」，本地 Windows 曾是「16 通过 / 1 警告」——差异来自状态而非平台：
> 3 笔签批完成后，「队列有单号、台账未签批」那条警告自然消失。

## 3. 迭代史：CI 前三次是红的（这才是本次执行的价值）

| Run | 触发提交 | 结果 | 根因 |
| --- | --- | --- | --- |
| `36242300518` | 签批登记 | ❌ failure | `ENOENT: mkdtemp '/tmp/opencode/autodispatch-selftest-…'` —— selftest 依赖 `<tmp>/opencode` 存在，ubuntu runner 上没有 |
| `36242460971` | 1221 留档 | ❌ failure | 同上（未修） |
| `36243203683` | 1223 模拟留档 | ❌ failure | `Error: 已有 watcher 实例在运行（pid=2314…）` —— 锁测试在 Linux 上遇 zombie 竞态：`live.kill()` 后未等子进程退出，`process.kill(pid,0)` 对 zombie 仍返回成功 |
| `36243238096` | 382a8364 | ✅ success | — |

## 4. 本次执行修掉的三个「本地跑通 ≠ CI 跑通」问题

| # | 问题 | 平台差异 | 修复 | 防回归 |
| --- | --- | --- | --- | --- |
| 1 | `mkdtemp(<tmp>/opencode/…)` 未确保父目录 | Windows 上该目录恰好存在，ubuntu 上不存在 | 先 `mkdir(recursive)` 再 mkdtemp | CI 本身即回归网 |
| 2 | validate 硬编码 `---\n` / `/^mode: subagent$/m` / `split('\n')` | CRLF 检出下 20 个专家 Agent 全部误报、RACI 逐格比对失效 | 全部文本读取统一走 `rd()` 归一化 CRLF→LF | 干净克隆模拟（6/6） |
| 3 | 锁测试 `live.kill()` 未等退出 | Linux zombie 窗口内 pid 仍「存在」 | `await` 子进程 `exit` 事件后再继续 | CI 本身即回归网 |

## 5. 条件判定

| 条件项 | 状态 |
| --- | --- |
| CI workflow 已入库并自动触发 | ✅ `.github/workflows/expert-guardrails.yml` |
| 6 项确定性检查在真实 runner 全绿 | ✅ run `36243238096` = success |
| 红→绿迭代过程留痕 | ✅ 本文件 §3 |

**结论：1223 的「CI 首次跑绿」条件已满足，可关闭。**（关闭动作由人类在 `审批记录.md` 的状态/备注中确认。）

> 后续维护提示：CI 是唯一能同时覆盖「Linux + Node 22 + 干净检出」三个差异面的回归网；
> 任何脚本改动都应先本地过 6 项，再看 CI 是否绿——本地绿不代表 CI 绿。
