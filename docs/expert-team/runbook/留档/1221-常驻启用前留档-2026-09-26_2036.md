# 1221 常驻启用前留档

> 依据：`AP-20260925-1221`（watcher 本地化运行可靠性与常驻启用，签批结论「有条件批准」）
> 条件：**先执行 `--once --dry-run` 与 `selftest` 并留档，再常驻启用**。本文件即为该条件的执行证据。
> 留档时间：2026/9/26 20:36:01　·　留档人：duxiangli（代为执行，结论待人类确认）

## 1. 环境指纹

| 项 | 值 |
| --- | --- |
| Node | v26.9.0 |
| 平台 | win32 x64 |
| 仓库 | main @ 79bffda |
| 状态文件 | `%APPDATA%\ai.opencode.desktop\autodispatch-state.json` |

## 2. 自动化自测（门禁：必须全过）

命令：`node scripts/selftest.mjs`　·　退出码：0

```

[1] 参数解析（--flag=value 与 --flag value 等价）
  ✅ 布尔开关
  ✅ 空格形式与等号形式 interval 一致 → 30
  ✅ 空格形式与等号形式 max-commits 一致 → 10
  ✅ interval 取值 → 30
  ✅ 中文参数值 → "冒烟标题"
  ✅ 路径参数两种写法 → ["C:/x","C:/x/s.json"]
  ✅ 未知参数进入告警而非静默
  ✅ 非法 interval 值被拒
  ✅ 过小 interval 被提升到下限 5s → 5
  ✅ reset-baseline / replay-last 开关

[2] 不可信输入清洗（提示注入面）
  ✅ 控制字符被清除 → "a b c"
  ✅ bidi 覆写字符被清除 → false
  ✅ 标签闭合字符被替换 → "x ＜ y ＞ z"
  ✅ 换行折叠防伪造日志行 → "line1 line2"
  ✅ 超长主体截断到 120+省略号 → 121
  ✅ null 安全 → ""

[3] 不可信数据块封装
  ✅ 块起始标记
  ✅ 块结束标记
  ✅ 注入文本无法伪造额外标记块 → 2
  ✅ 变更文件入块
  ✅ 超长数据被预算截断（实际 4009 字符）

[4] 提示词安全边界
  ✅ 声明输入不可信
  ✅ 明确禁止执行块内指令
  ✅ 治理边界保留
  ✅ 提醒签批台账仅人类可写
  ✅ 要求按变更路径判定风险面

[5] 增量分批计划（无缺口无重复）
  ✅ 单批上限生效 → 3
  ✅ 溢出计数 → 4
  ✅ lastHead 推进到本批最旧一条 → "h2"
  ✅ 新→旧排序，最新为 h0 → "h0"
  ✅ 不足上限时全派并推进到最旧 → [7,0,"h6"]
  ✅ 空输入安全 → 0

[6] 状态 fail-closed 与原子写
  ✅ 首次读取为空状态（不视为基线已建） → 0
  ✅ 状态含版本号 → 2
  ✅ 状态往返一致 → "abc"
  ✅ 不把内部字段写进状态文件 → undefined
  ✅ 状态文件已落盘
  ✅ 状态损坏 → fail-closed 抛错（不静默重建基线）
  ✅ --state 优先于默认路径 → "X"

[7] 状态 v1 → v2 迁移
[2026-09-26 12:36:01] state: 已将 v1 结构迁移为 v2（repos 包装 + dispatches 审计）
  ✅ v1 顶层键被收进 repos
  ✅ 迁移时补齐 dispatches 审计字段 → []
  ✅ v1 基准 lastHead 保留 → "y"

[8] 单实例锁（并发重复派单防护）
  ✅ 活着的其他进程被拒（pid=36828）
  ✅ process.kill(pid,0) 在本机可正确识别存活进程
[2026-09-26 12:36:01] lock: 陈旧锁（pid=36828 已退出），本次接管
  ✅ 锁记录写入本进程 pid → 34492
[2026-09-26 12:36:01] lock: 陈旧锁（pid=33804 已退出），本次接管
  ✅ 陈旧锁（pid 已退出）可被接管
  ✅ 释放锁后锁文件消失

[9] 回归：git 路径不做八进制转义（否则路径路由规则全部失配）
  ✅ 输出不含八进制转义序列（core.quotepath=false 生效）
  ✅ 中文文件名以真实字符返回（可被路径路由规则匹配）

[10] 回归：dry-run 不写盘（只报告、不改任何状态）
[2026-09-26 12:36:01] state: 检测到 v1 结构（dry-run 不写盘，未迁移）
  ✅ dry-run 仍在内存中完成 v1→v2 迁移
  ✅ dry-run 未把迁移结果写回文件 → "{\"C:/repo\":{\"seenHashes\":[\"x\"],\"lastHead\":\"x\",\"lastCheck\":\"t\"}}"
[2026-09-26 12:36:01] state: 已将 v1 结构迁移为 v2（repos 包装 + dispatches 审计）
  ✅ 非 dry-run 才会落盘迁移

结果：52 通过 / 0 失败
```

## 3. 派单 dry-run（门禁：不得派单，只报告将派什么）

命令：`node scripts/autodispatch-watcher.mjs --once --dry-run`　·　退出码：0

```
[2026-09-26 12:36:01] cli=C:\Users\duxiangli\AppData\Roaming\ai.opencode.desktop\cli\2.0.18\opencode-cli.exe
[2026-09-26 12:36:01] repo=C:\Users\duxiangli\Documents\Default Project
[2026-09-26 12:36:01] mode=单次  dryRun=true  mockMr=-  maxCommits=50
[2026-09-26 12:36:02] dry-run：窗口新提交 4 条，本批将派 4 条，溢出 0 条
[2026-09-26 12:36:02]   [will-fire] 79bffda9 签批登记：AP-1221/1222/1223 三笔均「有条件批准」（签批人 duxiangli），未闭环清零
[2026-09-26 12:36:02]   [will-fire] 5f5ff30b 专家团 v2 审计+升级：门禁可判定、路由确定性、闭环可追踪、漂移可检出
[2026-09-26 12:36:02]   [will-fire] b48734f1 审计留痕：router冒烟派单三笔记录入库（DSP-20260925-1221~1223：watcher本地化评审→需人工/驳回/需人工，均待签批，人类A签批后回填审批记录）
[2026-09-26 12:36:02]   [will-fire] bd44056b 专家团交付包导出：新增 export-bundle.mjs(纯Node zip写入，规避PowerShell中文转码与Compress-Archive丢隐藏目录两坑，自检+CRC+无./前缀) + 包内导入说明源文件 + .gitignor…
[2026-09-26 12:36:02]   变更文件 46 个（前 10）：docs/expert-team/runbook/审批记录.md, docs/expert-team/runbook/度量看板.md, docs/expert-team/runbook/待签批清单.md, docs/expert-team/runbook/签批状态视图.md, scripts/dispatch-metrics.mjs, .github/workflows/expert-guardrails.yml, .opencode/agents/router.md, README.md, docs/expert-team/03-跨域RACI.md, docs/expert-team/04-编排与门禁.md
[2026-09-26 12:36:02] ---- 提示词预览 ----
【事件推送·自主评审】本地仓库检测到新提交 4 条：
79bffda9 签批登记：AP-1221/1222/1223 三笔均「有条件批准」（签批人 duxiangli），未闭环清零
5f5ff30b 专家团 v2 审计+升级：门禁可判定、路由确定性、闭环可追踪、漂移可检出
b48734f1 审计留痕：router冒烟派单三笔记录入库（DSP-20260925-1221~1223：watcher本地化评审→需人工/驳回/需人工，均待签批，人类A签批后回填审批记录）
bd44056b 专家团交付包导出：新增 export-bundle.mjs(纯Node zip写入，规避PowerShell中文转码与Compress-Archive丢隐藏目录两坑，自检+CRC+无./前缀) + 包内导入说明源文件 + .gitignor…

<<<UNTRUSTED_COMMIT_DATA
commit 79bffda9 | 签批登记：AP-1221/1222/1223 三笔均「有条件批准」（签批人 duxiangli），未闭环清零
commit 5f5ff30b | 专家团 v2 审计+升级：门禁可判定、路由确定性、闭环可追踪、漂移可检出
commit b48734f1 | 审计留痕：router冒烟派单三笔记录入库（DSP-20260925-1221~1223：watcher本地化评审→需人工/驳回/需人工，均待签批，人类A签批后回填审批记录）
commit bd44056b | 专家团交付包导出：新增 export-bundle.mjs(纯Node zip写入，规避PowerShell中文转码与Compress-Archive丢隐藏目录两坑，自检+CRC+无./前缀) + 包内导入说明源文件 + .gitignor…
--- changed files ---
  docs/expert
```

## 4. 判读

- 自测退出码 0：全部断言通过，状态机与注入防护不变量成立
- dry-run 退出码 0：未创建任何会话、未写任何台账（dry-run 语义正确）
- dry-run 清单列出的「新提交」正是 1221 所指的历史漏派风险面：v1 的 200 条窗口会静默吞掉这段提交，
  v2 的 `lastHead..HEAD` 增量把它们全部显式列出（这正是本次加固要证明的行为）。

## 5. 本次留档发现并已修复的缺陷

留档不是走过场——它当场抓出两个真实缺陷，均已修复并补了回归断言：

| # | 缺陷 | 影响 | 修复 | 回归断言 |
| --- | --- | --- | --- | --- |
| 1 | `git show --name-only` 对非 ASCII 路径输出八进制转义（`docs/expert-team/runbook/\345\256\241...`） | **致命**：`raci/路径路由规则.csv` 的正则全部匹配失败 → 确定性强制路由静默失效，router 拿到的路径是乱码 | `git()` 统一加 `-c core.quotepath=false` | `selftest` [9] 组 |
| 2 | `--dry-run` 仍把 v1→v2 状态迁移写回磁盘 | 违反「dry-run 只报告不改任何东西」语义，污染审计基线 | `loadState(flags, {persist: !dryRun})` | `selftest` [10] 组 |

> 教训：这两类问题靠人眼看文档是发现不了的，只能靠「跑一遍并把原始输出留档」。

## 6. 放行判断（待人类确认）

| 条件项 | 状态 |
| --- | --- |
| selftest 全过 | ✅ 已满足 |
| dry-run 留档 | ✅ 已满足 |
| 常驻启用 `--interval 60` | ⬜ 待人类决定（本次未启动常驻） |

> 提醒：常驻期间每次新提交都会自动创建 router 会话并写派单日志。
> 若模型通道仍不可用（会话 tokens=0），会出现「会话已建但无结论」，派单日志无新增即为未留痕。
